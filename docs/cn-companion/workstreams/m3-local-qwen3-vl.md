# m3-local-qwen3-vl

- Owner: Local VLM Owner / Integration Owner
- Status: ready / device-verified local slice — Windows Ollama 保持 `degraded`；Transformers NF4、Desktop main worker、定向 Eventa gateway/facade、可信 consent、持续指示、single-owner、context projection 与真实 UI 均已通过
- Scope: M3.6 Qwen3-VL-4B-Instruct local runtime identity, strict completed-response parsing, loopback health, structured image inference, cancellation, resource guard and real-hardware evidence
- Files: local-screen adapter/parser/runtime/tests under `packages/stage-ui/src/services/perception/**`；loopback worker 位于 `services/perception-qwen3-vl-transformers/**`；main manager/gateway 位于 `apps/stage-tamagotchi/src/main/services/airi/perception/**`；shared contract 位于 `apps/stage-tamagotchi/src/shared/eventa/perception-local-screen.ts`；renderer facade 位于 `apps/stage-tamagotchi/src/renderer/services/perception/local-screen-eventa-runtime.ts`；真实实测脚本位于 `apps/stage-tamagotchi/scripts/perception-local-model-spike/**` 与 `perception-transport-spike/**`
- Depends on: completed M3.5 scheduler, frozen `perception/v0.3`; model download/runtime resource decision gate passed on 2026-07-16
- Contract version/commit: `perception/v0.3`; no commit requested
- Safety boundary: only bounded facts enter the existing trusted context projection; no direct chat/TTS/tool/Live2D wiring, no raw frame persistence, loopback only, Instruct identity required, strict completed JSON only
- Verification: strict parser/runtime/controller tests reject Markdown recovery、额外字段、tool-like event、非法 enum/summary、Thinking identity、非 loopback endpoint、revision/quantization/capability 不匹配、generation/source/frame metadata 不匹配；验证 single in-flight、Abort/timeout、真实 JPEG 边界、strict schema、唯一 `PerceptionStateManager` 写入口、停止撤回事实、稳定错误码与幂等卸载。Transformers NF4 真实单图分别返回 `code`/0.98、`browser`/0.90；多图 `[code,browser] -> browser`、`[browser,code] -> code`；真实 facade 取消通过，stop 后端口和 GPU compute process 均为 0；E2E 推理约 3.9–4.6 秒，峰值总显存约 7,506MiB。Desktop main manager 另一次真实启动 load 12,441ms、双图 5,612ms；Electron Eventa 4×190KiB 成功路径 7.9ms 且 Abort 返回 `AbortError`。当前完整 M3 domain/service 111/111、Desktop Eventa/manager/facade targeted 15/15、根级 `pnpm typecheck` 和 `pnpm lint` 均通过
- Handoff notes: 官方 revision 与两个 safetensors 分片已完整 SHA-256 固定，隔离 runtime 和缓存位于 WSL 用户目录，不占项目目录；下载残留、合成测试图、临时 token/log 已清理。Desktop main manager 实测 load 12,441ms、双图推理 5,612ms 并正确采用最后一帧；stop 后端口、worker、Python/WSL GPU client 均清零。Electron Eventa 实测 4×190KiB/7.9ms，Abort 返回 `AbortError`。生产设置页只有逐会话 consent 后才 lazy-start；持续指示、privacy pause、stop/revoke、single-owner Web Lock、generation 撤回与固定 context projection 已在真实 Electron 用户路径通过。vLLM 保留第二顺位，不自动安装或 fallback
