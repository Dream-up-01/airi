# M3.10 Minecraft Trusted Perception

- Status: partial — production provider, adapter, Stage integration, settings acceptance and generic third-party plugin host-ceiling acceptance implemented; a live authenticated Minecraft server run remains
- Owner: Codex `/root`
- Contract: `minecraft:perception:v1` carried only in `context:update.content`; external `text` is ignored
- Production path: authenticated registry identity -> strict schema/size/skew/rate/replay adapter -> `ObjectivePerceptionEvent` -> `PerceptionStateManager` -> bounded trusted projection
- Consent: disabled by default; the Minecraft settings page grants only this application session and provides an explicit stop/revoke action
- Privacy changes: removed complete `context:update`/`spark:command` payloads and Minecraft free text from Pinia and UI traffic history; the legacy free-text lane is reported only as `unstructured-context-ignored`
- Implemented rejection gates: exact registered runtime identity, schema version, strict fields, 4 KiB payload ceiling, enum allowlists, 30 s past/5 s future skew, event-id and monotonic-sequence replay protection, 10 events/s ceiling, source health, consent and generation isolation
- Lifecycle: unhealthy, disconnect, deannounce, revoke, expiry and runtime replacement retract or isolate prior facts; repeated healthy notifications do not rotate a live generation
- Provider: `services/minecraft` now emits only four allowlisted structured facts (`connection-health`, `player-status`, `task-state`, `nearby-threat`) with sequence, TTL and bounded enum values; disconnect emits `ended` facts, and identity/server/position/master data is not sent through the perception lane
- SDK boundary: the client handshake can declare the optional `perception.minecraft.structured` capability; the generic plugin host now has acceptance evidence that an extension grant limited to `wait` removes a module's requested `snapshot` action and unrelated camera capability
- Automated evidence: Minecraft/perception/context target suite 17/17; provider bridge/context 5/5; full `services/minecraft` Vitest 30/30 files and 236/236 tests; SDK client 11/11; plugin host/permission suite 38/38; Minecraft package, Stage UI, i18n, plugin protocol and SDK typechecks pass; targeted lint and production privacy scan pass
- Desktop acceptance (2026-07-16): opened `#/settings/modules/gaming-minecraft` in the real Electron renderer, confirmed the service-offline state does not prevent session consent, clicked `允许本次会话感知` and observed the control change to `停止并撤销授权`, then revoked and enabled again; the runtime store contained no payload/context-text field
- UI correction found by acceptance: the consent control now reads the Pinia store directly so setup-store mutations invalidate the renderer immediately instead of showing a stale label
- Package baseline repair (2026-07-18): aligned the workspace `vec3` catalog with Mineflayer's `0.1.10` contract; replaced shared-row matrix construction in map rendering and Levenshtein matching; restored the no-timeout LLM-agent call contract; added bounded audit logs for fired and rejected out-of-order detector decisions; widened one non-blocking worker timing assertion to tolerate parallel CI load. Full package typecheck, tests, scoped lint and whitespace checks now pass.
- Release evidence (2026-07-18): root `pnpm typecheck` passed; root `pnpm lint` passed with 0 errors; `@proj-airi/stage-tamagotchi` production build passed; touched-file whitespace and raw-data scans passed
- Fabric hardening (2026-07-23): pairing signatures bind protocol version and random per-process module instance ID; persisted device IDs are no longer runtime identities; terminal pairing/auth/protocol failures no longer reconnect; Windows private-key files receive an explicit owner-only ACL.
- Revocation/conflict closure (2026-07-23): revoking a paired device de-announces its authenticated module with `pairing-revoked`, removes the peer, closes the socket and lets the existing Stage disconnect path retract facts. Multiple valid `minecraft-bot` registry entries now fail closed with `minecraft-provider-conflict`; no provider is selected until one remains.
- Hardening evidence (2026-07-23): server-runtime 8/8, Desktop pairing manager 1/1 and Stage UI Minecraft store 6/6 targeted tests passed; affected package typechecks and root `pnpm typecheck` passed. Fabric `clean test check build` passed 19 tests across 10 suites twice with reproducible release SHA-256 `9b359fb94665a183956eb86be40ae7cf8a3ab861d23f153c893a416879247c10`. Root lint was initially blocked by the ignored `.hermes-control/binary-metadata.json` artifact and is now clean after the generated-file ignore and document-heading fixes; the current run reports 416 existing warnings and 0 errors.
- Renderer owner lifecycle (2026-07-27): only the main Stage renderer can own Minecraft perception resources; settings renderers send start/pause/resume/stop through a validated BroadcastChannel control contract. Settings teardown no longer stops the main owner; the control peer regression suite passes 2/2, Stage UI Minecraft store passes 7/7, root typecheck passes, and root lint passes with 0 errors.
- Local pairing probe (2026-07-27): with the AIRI channel listening on `ws://127.0.0.1:6121/ws`, the Java 21 `liveAiriProbe` completed the Ed25519 hello/proof exchange and reached the pending approval gate (`live-airi-probe:pairing-gate-reached`). No Minecraft process was running, so this does not count as real PCL authenticated provider acceptance.
- Live PCL transport (2026-07-27): a PCL-launched Java 21 Minecraft process (PID observed locally) loaded Fabric 1.21.1/Loader 0.19.3 with `airi_mc_connect 0.1.0-alpha.1`; its loopback TCP connection to AIRI `127.0.0.1:6121` remained established during inspection. AIRI has a persisted paired-device entry and the current WebSocket peer has not emitted an unauthenticated pairing timeout. This proves the live paired transport reaches the channel, but no renderer-instrumented `connection-health`, `player-status` or `nearby-threat` fact has yet been captured, so provider-to-context acceptance remains pending.
- Remaining: a live authenticated Minecraft server/device run proving the production provider-to-Desktop path against a real registry identity and transport
