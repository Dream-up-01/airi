# M3.12 Production Perception Observability

- Status: production-integrated / external-acceptance-pending — local Screen/Camera/Minecraft 和 cloud presentation、跨 renderer 安全投影均已实现；云 Provider 与 Minecraft 实服仍需外部验收
- Owner: Codex `/root`
- Scope: unified local-source control surface, persistent indicators, pause-all, bounded fact inspector, explicit fact confirmation/retraction/clearing, cross-renderer owner/status/projection sync, lifecycle-safe UI refresh
- Files: `packages/stage-ui/src/domains/perception/observability*`, `packages/stage-ui/src/stores/modules/gaming-minecraft*`, `apps/stage-tamagotchi/src/{shared/eventa,renderer/components/perception,renderer/composables/perception,renderer/services/perception}/`, tamagotchi stage i18n
- Safety boundary: no raw frame, OCR, model summary, event/observation ID, full response, local path or secret may enter the presentation projection
- Automated evidence: prior local-source suites plus 11/11 targeted projection/Minecraft store tests, 7/7 Eventa status/projection contract and cross-renderer sync tests, and 2/2 Chromium control-surface tests; Desktop and i18n typechecks pass on 2026-07-17
- Real Electron evidence (2026-07-16): selected the external ChatGPT window and ran `Qwen/Qwen3-VL-4B-Instruct` locally; the persistent indicator, fixed 1280x720 / 0-1 FPS profile, sample/accept/drop/fact counters and bounded fact inspector were visible. Age and TTL advanced against the live clock. Confirming a fact changed it to `user-confirmed`; retracting the accepted fact immediately changed accepted count to zero and removed `system:trusted-perception`; a later fact republished context and clearing the session immediately removed it again. Privacy pause left zero fresh facts and no trusted context. Stop removed the persistent indicator, trusted context and the loopback model listener on port 39273. No raw image or free model response appeared in the UI.
- Real local-camera evidence (2026-07-16): the normal Settings user path now exposes a localized `可信感知` entry. From that page the user can switch to Camera, provide the session-only local grant, start all three analyzers, inspect accepted/suppressed facts with confidence/age/TTL/source, and confirm/retract/clear facts. MediaPipe, OpenCV.js 4.13.0 and YOLOX-Nano all reached `ready`; the run sustained 409 working frames and six fresh facts in the settings acceptance pass. Pause synchronously changed all analyzers to stopped and fresh facts to zero. Stop from either the page or persistent indicator revoked the grant, cleared the consent checkbox and fact inspector, released the lanes, and ignored/cleared late analyzer errors.
- Real cross-window evidence (2026-07-16): ran local Camera in the Settings renderer while the main renderer stayed open. Exactly one `airi:perception:production-owner:v1` Web Lock was held with zero pending locks. The main renderer showed a persistent camera/active indicator, exposed only bounded source/state, disabled screen and camera consent/start controls, and did not receive frames, facts, source ids or Provider output. Stopping in the owner renderer released the lock and removed the remote indicator/read-only state within the 3.5-second heartbeat TTL. Evidence: `qa-multiwindow-remote-indicator.png`, `qa-multiwindow-read-only-panel.png`, `qa-multiwindow-after-stop.png`.
- Minecraft UI/integration evidence (2026-07-17): the production surface exposes a third source tab with explicit session consent, authenticated runtime health, accepted/rejected/suppressed counts, sanitized rejection code, fact confirm/retract/clear, pause/resume and revoke. It shares the production Web Lock with Screen/Camera, publishes only exact-key runtime status to non-owner windows, and sends only strict fixed-template context projections over a separate Eventa channel. Browser tests verify explicit consent and pause-all orchestration; store tests verify pause generation isolation and stale-event rejection.
- Cloud/Electron evidence (2026-07-18, policy update 2026-07-28): Settings 云面板真实点击“验证配置状态”后显示配置检查完成且云上传未启动；API Key、Workspace、模型权限与 WebSocket transport 有效，未创建 Provider session。2026-07-27 官方售后确认一个月服务日志、断连清除模型会话上下文、no-training/no-improvement/no-evaluation/no-human-review 和中国内地数据边界后，UI/wire policy 已更新至 v0.4 并保持政策字段、readiness evidence 与 uploadActive 分离。300px 主窗口中的感知浮窗不再逐字换行；HMR/重挂载后的 BroadcastChannel 可销毁重建且控制台无 `Channel is closed`。截图位于 `perception/qa-round2/screenshots/`。
- Remaining: 日期化 Provider policy evidence 已写入 Git 忽略的 Desktop `.env.local`；v0.4 readiness/双授权/政策文案已通过真实 production Electron 复验，仍需在 Workspace、API Key 和模型可用性配置重新注入后进行有界真实付费 Provider、费用、stop/revoke/generation 验收。Minecraft 需启动 AIRI channel 与 PCL/Fabric 1.21.1 客户端，在单人或多人世界中完成已配对 Mod 的 structured fact、disconnect/TTL/revoke 验收；client-only Mod 不要求开放局域网，PCL/离线用户名/游戏服务器身份不构成授权。官方尚未回答日志精确载荷范围及一个月留存能否缩短，文档和 UI 不得声称零留存。
